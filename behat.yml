default:
    suites:
        cantine_users:
            paths:
                - %paths.base%/features/cantine
            contexts:
                - StudentContext
            filters:
                role: student
        cantine_admins:
            paths:
                - %paths.base%/features/cantine
            contexts:
                - AdminContext
            filters:
                role: admin
